export default function Home() {
  return (
    <main style={{ fontFamily: "ui-monospace, monospace", padding: "3rem", lineHeight: 1.7 }}>
      <h1>Naap</h1>
      <p>Telegram macro tracker. The bot is the interface — this page is just the deployment.</p>
      <p>Webhook: <code>/api/telegram</code></p>
    </main>
  );
}
